# Per-machine monitor config (stable output names keep serials out of git)

# Samsung ultrawide primary (DP-2), left side
monitor = DP-2, 5120x1440@239.76, 4596x271, 2

# Rotated portrait utility monitor (DP-3), right of the ultrawide.
# Auto-position only — DO NOT specify mode/scale/transform here. Hyprland's
# reload path triggers a DRM disconnect on DP-3 while changing modes
# (verified in aquamarine log) and silently drops the transform. The
# transform is applied at runtime instead via
# scripts/hypr-sync-plugin-binds.sh (runs as `exec =` after plugin load,
# and re-runs on every config reload).
monitor = DP-3, preferred, auto, 1

# No catch-all wildcard — it otherwise resets scale + transform on every
# reload. Hotplug of unknown monitors will use Hyprland's compiled-in
# defaults (preferred mode, scale=1), which is acceptable.

# Workspace orientation defaults (master layout fallback)
# split-monitor-workspaces count=5: ws 1-5 → DP-2, ws 6-10 → DP-3
# Ultrawide landscape (DP-2): ws 1-5
workspace = 1, layoutopt:orientation:left
workspace = 2, layoutopt:orientation:left
workspace = 3, layoutopt:orientation:left
workspace = 4, layoutopt:orientation:left
workspace = 5, layoutopt:orientation:left
# Portrait utility (DP-3): ws 6-10
workspace = 6, layoutopt:orientation:top
workspace = 7, layoutopt:orientation:top
workspace = 8, layoutopt:orientation:top
workspace = 9, layoutopt:orientation:top
workspace = 10, layoutopt:orientation:top
