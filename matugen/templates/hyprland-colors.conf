# Generated by render-palette.sh — do not edit by hand.
# Sourced from hyprland.conf via `source = ~/.config/hypr/colors.conf`
# Source palette: theme/palette.env

$theme_primary   = rgba(${THEME_PRIMARY}ee)
$theme_secondary = rgba(${THEME_SECONDARY}ee)
$theme_tertiary  = rgba(${THEME_TERTIARY}ee)
$theme_warning   = rgba(${THEME_WARNING}ee)
$theme_danger    = rgba(${THEME_DANGER}ee)
$theme_border    = rgba(${THEME_BORDER}cc)
$theme_fg        = rgba(${THEME_FG}ff)
$theme_bg        = rgba(${THEME_BG}ff)
$theme_panel     = rgba(${THEME_PANEL}dd)
$theme_surface   = rgba(${THEME_SURFACE}dd)

# Shadow color (dark bg with alpha)
$theme_shadow          = rgba(${THEME_BG}88)
$theme_shadow_inactive = rgba(${THEME_BG}66)
