#%PAM-1.0

auth       required     pam_securetty.so
auth       requisite    pam_nologin.so
auth       include      system-local-login
auth       [success=1 default=ignore] pam_succeed_if.so user = greeter quiet
auth       optional     pam_gnome_keyring.so
account    include      system-local-login
session    include      system-local-login
session    [success=1 default=ignore] pam_succeed_if.so user = greeter quiet
session    optional     pam_gnome_keyring.so auto_start
password   [success=1 default=ignore] pam_succeed_if.so user = greeter quiet
password   optional     pam_gnome_keyring.so
