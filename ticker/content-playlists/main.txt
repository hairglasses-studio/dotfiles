# Ticker content playlist — one stream ID per line
# Read by keybind-ticker.py at startup. Lines starting with # are ignored.
# Each stream rotates according to its per-stream refresh interval
# (see STREAM_META in keybind-ticker.py).
keybinds
system
fleet
weather
github
notifications
music
updates
mx-battery
disk
load
workspace
