# Inherit model, reasoning, and personal preferences from ~/.codex/config.toml.
# The global launcher layer now enforces root + full-access execution; this
# repo-local template remains project-scoped for compatibility surfaces.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# BEGIN GENERATED MCP SERVERS: codex-mcp-sync
# Generated by codexkit/scripts/codex-mcp-sync.sh from .mcp.json and mcp-profile-policy.json
# Discovery-first dotfiles MCP profile for fleet and rice inspection.
[mcp_servers.dotfiles_review]
command = "bash"
args = ["-lc", "exec ./scripts/run-dotfiles-mcp.sh"]
enabled_tools = [
  "dotfiles_tool_search",
  "dotfiles_tool_schema",
  "dotfiles_tool_catalog",
  "dotfiles_tool_stats",
  "dotfiles_fleet_audit",
  "dotfiles_health_check",
  "dotfiles_dep_audit",
  "dotfiles_rice_check",
]

# Broader dotfiles MCP profile for repo operations and desktop reload workflows.
[mcp_servers.dotfiles_workspace]
command = "bash"
args = ["-lc", "exec ./scripts/run-dotfiles-mcp.sh"]
enabled_tools = [
  "dotfiles_tool_search",
  "dotfiles_tool_schema",
  "dotfiles_tool_catalog",
  "dotfiles_tool_stats",
  "dotfiles_fleet_audit",
  "dotfiles_health_check",
  "dotfiles_dep_audit",
  "dotfiles_pipeline_run",
  "dotfiles_bulk_pipeline",
  "dotfiles_create_repo",
  "dotfiles_onboard_repo",
  "dotfiles_cascade_reload",
  "dotfiles_rice_check",
]

# Desktop-first dotfiles MCP profile for Hyprland, OCR, input control, and the kitty terminal visual pipeline.
[mcp_servers.dotfiles_desktop]
command = "bash"
args = ["-lc", "exec ./scripts/run-dotfiles-mcp.sh"]

[mcp_servers.dotfiles_desktop.env]
DOTFILES_DIR = "/home/hg/hairglasses-studio/dotfiles"
DOTFILES_MCP_PROFILE = "desktop"

# END GENERATED MCP SERVERS: codex-mcp-sync

[profiles.readonly_quiet]
approval_policy = "never"
sandbox_mode = "read-only"
model_reasoning_effort = "medium"

[profiles.review]
approval_policy = "on-request"
sandbox_mode = "read-only"
model_reasoning_effort = "high"

[profiles.workspace_auto]
approval_policy = "on-failure"
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"

[profiles.ci_json]
approval_policy = "never"
sandbox_mode = "workspace-write"
model_reasoning_effort = "medium"

# BEGIN GENERATED OLLAMA PROFILES: provider-settings-sync
# Generated by codexkit/scripts/provider-settings-sync.sh from dotfiles shared local-model defaults.
# Refresh this block after updating dotfiles Ollama defaults or host settings.

[model_providers.ollama_local]
name = "Local Ollama"
base_url = "http://127.0.0.1:11434/v1"
env_key = "OLLAMA_API_KEY"
env_key_instructions = "source \"$HOME/hairglasses-studio/dotfiles/scripts/lib/hg-local-llm.sh\" && hg_local_llm_export_env"
requires_openai_auth = false
wire_api = "responses"

[profiles.ollama_chat]
model_provider = "ollama_local"
model = "code-primary"
model_reasoning_effort = "low"

[profiles.ollama_fast]
model_provider = "ollama_local"
model = "code-fast"
model_reasoning_effort = "low"

[profiles.ollama_code]
model_provider = "ollama_local"
model = "code-primary"
model_reasoning_effort = "medium"

[profiles.ollama_heavy]
model_provider = "ollama_local"
model = "code-heavy"
model_reasoning_effort = "high"

[profiles.ollama_high_context]
model_provider = "ollama_local"
model = "code-long"
model_reasoning_effort = "high"

[profiles.ollama_cloud_code]
model_provider = "ollama_local"
model = "glm-5.1:cloud"
model_reasoning_effort = "high"

[profiles.ollama_cloud_verified]
model_provider = "ollama_local"
model = "glm-5:cloud"
model_reasoning_effort = "high"

[profiles.ollama_multilingual]
model_provider = "ollama_local"
model = "minimax-m2.1:cloud"
model_reasoning_effort = "high"

[profiles.ollama_thinking]
model_provider = "ollama_local"
model = "kimi-k2-thinking:cloud"
model_reasoning_effort = "high"

# END GENERATED OLLAMA PROFILES: provider-settings-sync
