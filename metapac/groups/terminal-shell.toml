# Terminal emulators, shell, prompt, multiplexer
[arch]
packages = [
    # Terminals
    { name = "kitty", hooks = { after_sync = ["systemctl", "--user", "enable", "shader-rotate.timer"] } },
    { name = "foot" },

    # Shell
    { name = "oh-my-zsh" },

    # Prompt & history
    { name = "starship" },
    { name = "atuin" },
    { name = "zoxide" },

    # Multiplexer
    { name = "tmux" },
    { name = "zellij" },

    # Terminal effects
    { name = "terminaltexteffects-git" },
]
