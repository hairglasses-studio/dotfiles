# Terminal emulators, shell, prompt, multiplexer
[arch]
packages = [
    # Terminals
    { name = "kitty", hooks = { after_sync = ["systemctl", "--user", "enable", "shader-rotate.timer"] } },
    { name = "foot" },

    # Shell
    # oh-my-zsh: installed by install.sh via upstream curl script (AUR package renamed to oh-my-zsh-git; install.sh handles bootstrap directly)

    # Prompt & history
    { name = "starship" },
    { name = "atuin" },
    { name = "zoxide" },

    # Multiplexer
    { name = "tmux" },
    { name = "zellij" },

    # Terminal effects
    { name = "terminaltexteffects-git" },
]
