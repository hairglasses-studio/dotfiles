# NVIDIA: preserve video memory allocations across suspend/resume.
# Moved here from the kernel cmdline (cleaner location for module params).
# Required for clean Wayland compositor resume on RTX cards.
options nvidia NVreg_PreserveVideoMemoryAllocations=1
