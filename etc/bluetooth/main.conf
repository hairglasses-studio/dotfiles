[General]
# Keep only the explicit desktop defaults we rely on.
AlwaysPairable = true
FastConnectable = true
JustWorksRepairing = always

# Leave BlueZ experimental features disabled on this machine. They trigger
# startup noise for BAP/ISO/uinput paths we do not use in the desktop flow.
